# React + Vite

This is simple rating UI where you can submit your rating. This simple app is divided into components that is what react is about dividing UI into simple reusable components and it provides understanding of fundamental concepts of React such as JSX, Components, Conditional Rendering, Props, Children Props, States and much more


# Install Dependences and Run

npm install

npm run dev
